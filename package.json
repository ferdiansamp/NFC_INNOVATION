{
  "name": "kai-root",
  "private": true,
  "scripts": {
    "install": "cd Backend && npm install && cd ../frontend && npm install && npm run build",
    "start": "cd Backend && npm start"
  },
  "dependencies": {
    "cors": "^2.8.5"
  }
}
