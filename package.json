{
  "name": "kai-root",
  "private": true,
  "scripts": {
    "install": "cd Backend && npm install && cd ../frontend && npm install && npm run build",
    "start": "cd Backend && npm start"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "nfc-pcsc": "^0.8.1",
    "node-fetch": "^3.3.2",
    "pcsclite": "^1.0.1",
    "ws": "^8.18.3"
  }
}
